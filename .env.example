# Variables de entorno de ejemplo
# Copia este archivo a .env.local y completa con tus valores

# Ejemplo de variable pública de Next.js (accesible desde el navegador)
# NEXT_PUBLIC_API_URL=https://api.ejemplo.com

# Ejemplo de variable privada (solo servidor)
# API_SECRET_KEY=tu_secret_key_aqui

# Vercel Analytics (opcional, ya está incluido)
# NEXT_PUBLIC_VERCEL_ANALYTICS_ID=auto
